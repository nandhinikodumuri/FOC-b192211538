//17.Finding the whether given number is even or odd
#include<stdio.h>
int main()
{
int n;
printf("\n Enter a number:");
scanf("%d",&n);
if (n%2==0)
pprintf("%d is an even number.",n);
else
printf("%d is an odd number.",n);
return 0;
}
